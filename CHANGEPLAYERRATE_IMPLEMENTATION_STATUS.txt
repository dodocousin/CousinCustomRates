# ChangePlayerRate Redesign Implementation Summary

## STATUS: Implementation requires manual application due to file editing constraints

## Changes Needed:

### 1. Source/Public/CousinCustomRates.h
- Replace `struct TribeHarvestBoost` with `struct PlayerHarvestBoost`
- Change map key from `int` (teamId) to `string` (eosId)
- Add fields: isMultiplier, alsoForTribe, appliedByAdmin

### 2. Configs/config.json
- Add new ChangePlayerRate section

### 3. Source/TargetedRates.h  
- Rewrite ApplyTribeHarvestBoost -> ApplyPlayerHarvestBoost
- Add RemovePlayerHarvestBoost function
- Update harvest hook logic
- Add GetHarvestBoostContext function

### 4. Source/Utils.h
- Add ChangePlayerRate config validation
- Update state save/load functions

## Recommendation:
Due to complexity (~400 lines of changes), suggest:
1. Test PostLogin fix first
2. Implement ChangePlayerRate redesign in separate branch
3. Or provide Visual Studio project access for direct compilation

